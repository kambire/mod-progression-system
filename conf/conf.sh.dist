#!/usr/bin/env bash

## CUSTOM SQL
## This file is used by AzerothCore's optional db_assembler.sh tooling.
##
## IMPORTANT:
## This module applies SQL using AzerothCore's DBUpdater from bracket folders under:
##   modules/mod-progression-blizzlike/src/Bracket_*/sql/{world,characters,auth}
##
## This repository does not provide a root-level sql/{db}/{base,updates} layout.
## Leave these arrays empty unless you reorganize SQL to match db_assembler expectations.

## BASE SQL

DB_AUTH_CUSTOM_PATHS+=(
)

DB_CHARACTERS_CUSTOM_PATHS+=(
)

DB_WORLD_CUSTOM_PATHS+=(
)

## UPDATES

DB_AUTH_UPDATES_PATHS+=(
)

DB_CHARACTERS_UPDATES_PATHS+=(
)

DB_WORLD_UPDATES_PATHS+=(
)
